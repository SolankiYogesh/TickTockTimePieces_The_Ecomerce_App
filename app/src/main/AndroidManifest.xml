<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.syd.ticktocktimepieces">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        tools:ignore="AllowBackup">
        <activity android:name=".GooglePayActivity" />
        <activity android:name=".MyTeamsActivity" />
        <activity android:name=".VrutikActivity" />
        <activity android:name=".PratikActivity" />
        <activity android:name=".VrushabhActivity" />
        <activity android:name=".OrderDetailActivity" />
        <activity android:name=".MyOrdersActivity" />
        <activity android:name=".AboutActiviti" />
        <activity android:name=".DashBoardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity"/>
        <activity android:name=".CartActivity" />
        <activity android:name=".WatchDetailActivity" />
        <activity android:name=".WatchActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>